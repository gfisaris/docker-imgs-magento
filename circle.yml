machine:
  services:
    - docker

dependencies:
  post:
    - docker info
    - docker build -t $AWS_ACCOUNT_ID.dkr.ecr.eu-central-1.amazonaws.com/my_app$CIRCLE_SHA1 .

deployment:
  ecr:
    branch: master
    commands:
      - ./circle-ci/push-dckimg2ecr.sh
